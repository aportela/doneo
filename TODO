move api services on client from subcomponents to parent manager component (ex: projectPriorityForm, ProjectStatusForm...)
use https://github.com/imzbf/md-editor-v3 for markdown ?
add into markdown code language && copy
standarize date formats
upload concurrency : database is locked (5) (SQLITE_BUSY)
review tables on small screens (hide columns ?)
auto session reauth with setTimeout
add task times for spent hours
use naive ui ellipsis on long texts
remove exported (Uppercase) on not required structs
check missing indexes
check project permission duplicate role for same user
set global cache && pass as depending reference to handlers/services
review `db:xxxx" mappings
remove "non updatable" get funcs on repo/service/handler
move entity details to entity handler/service/repository (ex: GetProjectAttachments from attachment to project)
all add/update services return domain entity with new values
add interface on handlers

project attachment/task attachemnt same table  as project permissions (id vs not id)

use domain.DeletedError on get project/task deleted

Get funcs on entitys for updating Vs use current search item

fix route names (new handlers)

return entity on all add / update funcs

check missing 404 responses on frontend services

add handler interface funcs

deny close not found dialogs ??